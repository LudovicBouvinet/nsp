{% trans_default_domain 'FOSUserBundle' %}
    
    
    <script src='https://www.google.com/recaptcha/api.js'></script>
    
     <div class="popup-inscri">
        <div class="head"><img src="{{ asset('ressources/tete.png') }}">
            <h2>Inscription</h2><img src="{{ asset('ressources/plumeorange.png') }}"> </div>  
            
{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
            <h3>Informations du compte</h3>
            <p>
           
            <div class="">
          {# PSEUDO #}
          {# Génération du label. #}
          {{ form_label(form.username, "Votre pseudo", {'label_attr': {'class': 'label label-pseudo'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.username) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.username, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
        
        <div class="">
          {# EMAIL #}
          {# Génération du label. #}
          {{ form_label(form.email, "Votre adresse email", {'label_attr': {'class': 'label label-email'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.email) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.email, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
        
        <div class="">

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.plainPassword, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
</p>
       
       <div class="hebergement">
                <p class="annotation">
          {# LIEN #}
          {# Génération du label. #}
          {{ form_label(form.lienPhoto, "URL de votre photo", {'label_attr': {'class': 'label label-lien'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.lienPhoto) }}
                </p>
                <p class="annotation">OU</p>
                <p class="annotation">Héberger votre image </p>
                <p>
            {# Génération de l'input. #}
            {{ form_widget(form.lienPhoto, {'attr': {'class': 'form-nom'}}) }}
            <input type="file" value="Votre fichier">
                </p>
            </div>
            

        
                    <h3>Détails personnels</h3>
            <p>
        <div class="">
          {# PRENOM #}
          {# Génération du label. #}
          {{ form_label(form.prenom, "Votre prénom", {'label_attr': {'class': 'label label-prenom'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.prenom) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.prenom, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
        
                <div class="">
          {# NOM #}
          {# Génération du label. #}
          {{ form_label(form.nom, "Votre nom", {'label_attr': {'class': 'label label-nom'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.nom) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.nom, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
        
</p>
        
                <div class="">
          {# Date de Naissance  #}
          {{ form_label(form.dateNaissance, "Votre date de naissance", {'label_attr': {'class': 'label label-ddn'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.dateNaissance) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.dateNaissance, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
        
               
                    <h3>Options du compte</h3>
            <p class="news">
                <div class="">
          {# Newsletter #}
          {# Génération du label. #}
          {{ form_label(form.newsletter, "Newsletter", {'label_attr': {'class': 'label label-news'}}) }}

          {# Affichage des erreurs pour ce champ précis. #}
          {{ form_errors(form.newsletter) }}

          <div class="">
            {# Génération de l'input. #}
            {{ form_widget(form.newsletter, {'attr': {'class': 'form-nom'}}) }}
          </div>
        </div>
</p>
   
   <h3>Vérification</h3>
            <div class="g-recaptcha" data-sitekey="6LdTnxMTAAAAAER7A4FQzYcBPyi4Oa0bra54usjg"></div>
            
            
    <div>
        <input type="submit" value="Inscription" />
        <INPUT type="reset" name="reset" class="reset" value=" Reinitialiser les champs ">
    </div>

{{ form_end(form) }}
</div>
